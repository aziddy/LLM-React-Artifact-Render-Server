# Prisma database connection (SQLite)
DATABASE_URL="file:./prisma/artifacts.db"

# Auth credentials (each must be at least 5 characters)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme
JWT_SECRET=your-secret-key-here
